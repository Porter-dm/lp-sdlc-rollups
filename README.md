# zebra-sdlc-rollups
The main 'big box' zebra integration repository

This also contains the build pipeline, though the pipeline independently checks out the required version rather than using the chart that is local to it.  This is just to save us having 2 repos which would be confusing.